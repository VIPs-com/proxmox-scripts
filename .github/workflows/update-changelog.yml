📜 Atualiza workflow do CHANGELOG  
[x] Commit directly to the main branch  

name: Update CHANGELOG
on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'proxmox-postinstall-aurora-luna.sh'

jobs:
  update-changelog:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Setup Git
      run: |
        git config --global user.name "GitHub Actions"
        git config --global user.email "actions@github.com"
    - name: Generate CHANGELOG Entry
      run: |
        echo "## [$(date +'%Y-%m-%d')] - Atualização Automática" >> CHANGELOG.md
        echo "### Alterado" >> CHANGELOG.md
        echo "- Atualização do script via GitHub Actions" >> CHANGELOG.md
        echo "" >> CHANGELOG.md
    - name: Commit Changes
      run: |
        git add CHANGELOG.md
        git commit -m "📜 Atualização automática do CHANGELOG"
        git push
